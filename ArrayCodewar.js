// This shows you how to produce an array of every number leading up to the number input.
function arr(n){
    var newArr = [];
    for(var i = 0; i < n; i++){
      newArr.push(i);
    }
    return newArr;
  }